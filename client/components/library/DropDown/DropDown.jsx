import React from 'react';

function DropDown(props) {
  // local state
  // set defaultSelectText
  // keep track if options list is being displayed
  // keep track of the actual options list passed down as props

  // useEffect
  // on mount needs to populate the defualtSelectText and the options list from props

  return <div>This is the dropdown</div>;
}

export default DropDown;
